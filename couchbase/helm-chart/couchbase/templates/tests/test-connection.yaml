apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "couchbase.fullname" . }}-test-connection"
  labels:
    {{- include "couchbase.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "couchbase.fullname" . }}:{{ .Values.couchbasedbinternal.service.port }}']
  restartPolicy: Never
